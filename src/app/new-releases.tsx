import ScreenPlaceholder from '@/components/common/ScreenPlaceholder';

/** 웹 NewReleasesPage 자리 — 홈의 "전체보기"가 여기로 온다. */
export default function NewReleasesScreen() {
  return <ScreenPlaceholder title="새로 나온 음반" />;
}
