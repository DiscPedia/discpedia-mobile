import ScreenPlaceholder from '@/components/common/ScreenPlaceholder';

/** 웹 UsedAlbumsPage 자리 — 홈의 "더보기"가 여기로 온다. */
export default function UsedAlbumsScreen() {
  return <ScreenPlaceholder title="중고 거래 음반" />;
}
