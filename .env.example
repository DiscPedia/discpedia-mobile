# API 서버 주소 (미설정 시 api-config.ts의 기본값 사용)
EXPO_PUBLIC_API_BASE_URL=https://discpedia.roundtable02.com

# 개발용 로그인 우회 — 1이면 로그인 없이 탭 화면으로 들어간다.
# 백엔드 OAuth가 앱 스킴을 지원하기 전까지 화면 작업용. 배포 빌드에는 넣지 말 것.
EXPO_PUBLIC_DEV_BYPASS_AUTH=0
